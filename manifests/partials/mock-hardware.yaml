x-common: &common
  platform: ${PLATFORM}

services:
  bluetooth-manager:
    <<: *common
    image: nginx:1.30.0-alpine
    command: [ "nginx", "-g", "daemon off;"]
    volumes:
      - ./partials/bluetooth-mock.nginx.conf:/etc/nginx/conf.d/default.conf:ro
